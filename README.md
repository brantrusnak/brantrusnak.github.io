# brantrusnak.github.io

- Click [here](http://brantrusnak.github.io/) to view portfolio
- `npm install` in this repo and run `npm run dev:ts` and `npm run dev:scss` to generate changes in TypeScript and SCSS. 
